<% header = link_to('Todas las entradas', post_path) + " &raquo; #{@category.name}".html_safe %>
<%= render 'shared/content_title', title: header %>

<% @category.posts.each do |post| %>
	<%= render 'posts/post', post: post %>
<% end %>